# Cross-Language SDK Code Generation

```bash
prkdb codegen --server http://127.0.0.1:50051 --lang typescript --out ./generated/ts
```

If you are pointing codegen at the local gRPC endpoint created by `prkdb-cli serve`, use `http://127.0.0.1:50051`.
